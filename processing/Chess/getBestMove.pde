public int getBestMove(ArrayList<String[]> movelist) {
    for (int i = 0; i < movelist.size(); i++) {
         println(movelist.get(i));   
    }
    int mylength = split(movelist.get(0)[0],"|").length - 1;
    print("mylength = " + mylength);
    return 0;
}
